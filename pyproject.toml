[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "camping-alert"
version = "0.1.0"
description = "Coastal campsite availability alert system for Newark, CA"
requires-python = ">=3.11"
dependencies = [
    "httpx>=0.27",
    "playwright>=1.44",
    "schedule>=1.2",
    "python-dotenv>=1.0",
    "tenacity>=8.3",
]

[project.scripts]
camping-alert = "camping_alert.main:main"

[tool.setuptools.packages.find]
where = ["src"]

[project.optional-dependencies]
dev = [
    "pytest>=8.0",
    "pytest-asyncio>=0.23",
    "pytest-mock>=3.14",
]
